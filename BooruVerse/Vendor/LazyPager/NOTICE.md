# LazyPager (vendored)

Source: https://github.com/gh123man/SwiftUI-LazyPager  
License: MIT  

Vendored under `BooruVerse/Vendor/LazyPager` and wrapped in `#if canImport(UIKit)` so the shared multiplatform app target still builds for macOS (the upstream package is iOS-only).
